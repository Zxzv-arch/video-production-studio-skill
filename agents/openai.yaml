interface:
  display_name: "Video Production Studio"
  short_description: "Standalone complex video editing and motion workflows"
  brand_color: "#6D5DFB"
  default_prompt: "Use $video-production-studio to initialize a guided video project, iterate in draft mode, and promote through review to a verified master only when ready."
policy:
  allow_implicit_invocation: true
