interface:
  display_name: "Video Production Studio"
  short_description: "Speech-synced PiP, Remotion demos, motion, and delivery"
  brand_color: "#6D5DFB"
  default_prompt: "Use $video-production-studio to create a sentence-reactive talking-head explainer in which every spoken sentence drives dynamic typography, a presenter/PiP state, and a content-specific Remotion demonstration; use focused Three.js only for spatial meaning, keep Text-to-CAD optional and verified, and deliver editable scenes with captions, sound, and QA."
policy:
  allow_implicit_invocation: true
